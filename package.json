{
  "name": "pantry",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Offline-first pantry, grocery list, recipe and meal-planning PWA for mobile and desktop",
  "repository": {
    "type": "git",
    "url": "http://local_proxy@127.0.0.1:41729/git/Tony9091/Pantry"
  },
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "icons": "node scripts/gen-icons.mjs",
    "e2e": "npm run build && node scripts/e2e.mjs",
    "standalone": "npm run build && node scripts/build-standalone.mjs",
    "test:parse": "node scripts/test-parse.mjs",
    "test": "npm run test:parse && npm run e2e && npm run test:offline",
    "test:offline": "node scripts/test-offline.mjs"
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.0.5",
    "playwright": "^1.62.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.0"
  }
}
